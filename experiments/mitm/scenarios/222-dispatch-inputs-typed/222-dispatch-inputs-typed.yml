name: 222-dispatch-inputs-typed
on: workflow_dispatch
jobs:
  dispatch:
    runs-on: self-hosted
    steps:


























      - run: |
          echo "log-level=${{ inputs.log-level }} dry-run=${{ inputs.dry-run }} note=${{ inputs.note }}"
          echo "shard=${{ matrix.shard }} event=${{ github.event_name }}"

  replay-extra-2:
    runs-on: self-hosted
    steps:
      - run: "echo \"replay placeholder 2\""
