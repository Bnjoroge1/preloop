name: 220-label-routing
on: workflow_dispatch
jobs:
  labeled:
    runs-on: self-hosted
    steps:
      - run: "echo \"routed by label list\""
      - run: "echo \"runner name=${{ runner.name }} os=${{ runner.os }} arch=${{ runner.arch }} temp=${{ runner.temp }}\""
