name: 224-matrix-include-exclude
on: workflow_dispatch
jobs:
  fail-fast-matrix:
    runs-on: self-hosted
    steps:






















      - run: |
          if [ "${{ matrix.shard }}" = "2" ]; then exit 1; fi
          echo "shard ${{ matrix.shard }} ok"
