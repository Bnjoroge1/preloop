# Official-runner golden sets replayed by benchmarks/conformance/run.sh.
# Add a target here instead of adding another CI job or another server.

[[targets]]
runner_version = "2.336.0"
exclude_prefix = ["2"]
